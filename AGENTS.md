# What's The Scoop — Dev Notes

This project uses TanStack Start (React SSR) with Tailwind CSS v4, deployed on Vercel.

## Dev
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## Deploy
Push to `main` — Vercel auto-deploys via GitHub integration.
