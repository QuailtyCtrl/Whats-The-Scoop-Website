// Error reporting stub — no external dependency
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // No-op: swap in your own error reporting (e.g. Sentry) here if desired
}
